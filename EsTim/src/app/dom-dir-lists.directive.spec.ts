import { RedOnHoverDirective } from './dom-dir-lists.directive';

describe('DomDirListsDirective', () => {
  it('should create an instance', () => {
    const directive = new RedOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
